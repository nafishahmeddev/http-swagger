import { createTheme } from "@mui/material";

export default createTheme({
    palette:{
        grey : {
            main: "#ccc",
            dark: "#666",
        }
    },
    shape:{
        borderRadius: 8,
    },
    typography:{
        fontFamily: '"Outfit", sans-serif'
    }
});